// src/components/MyOrders.jsx
import "../styles/Auth.css"; // o el css que uses para las páginas de auth

const MyOrders = () => {
  return (
    <div className="auth-page">
      <h2>Mis pedidos</h2>
      <p>Tu historial de pedidos aparecerá aquí próximamente.</p>
    </div>
  );
};

export default MyOrders;  // 👈 ESTE export default es lo que faltaba
